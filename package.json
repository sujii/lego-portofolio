{
  "name": "@repo/lego-portofolio",
  "private": true,
  "version": "0.9.1",
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "format": "turbo lint -- --fix",
    "web": "yarn workspace @repo/lego-portofolio-web",
    "api-client": "yarn workspace @repo/lego-portofolio-api-client",
    "pritty": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "prettier": "^3.4.1",
    "turbo": "^2.3.3"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "lisence": "GPL-3.0 license",
  "packageManager": "yarn@4.5.3"
}
